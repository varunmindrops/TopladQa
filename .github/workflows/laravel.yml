name: Laravel

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:

  deploy:
    runs-on: ubuntu-latest
  
    steps:
      - name: Deployment
        uses: appleboy/ssh-action@master
        with:
          host: 159.89.171.253
          username: root
          password: Raghav2020@Test
          script: |
            cd /var/www/html/
            git checkout -f 
            git pull origin main
            
